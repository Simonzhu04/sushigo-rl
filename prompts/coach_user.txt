STATE_JSON:
{{STATE_JSON}}

Use exactly the `action_index` values already present in `agent_recommendation`.
Provide exactly {{TOPK}} items.

For each item include:
- action_index
- reason
- tradeoff

Focus on pair/triple progress, maki race, pudding pressure, and wasabi-nigiri timing when relevant.

Return JSON only in this schema:
{
  "recommendations": [
    {"action_index": 0, "reason": "short state-grounded reason", "tradeoff": "short comparison to another legal option"}
  ]
}
